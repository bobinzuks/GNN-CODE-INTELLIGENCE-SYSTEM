[package]
name = "gnn-experts"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
petgraph = "0.6"
regex = "1.10"

# For dynamic expert loading
[dev-dependencies]
